#include<stdio.h>
const int maxn=10;
int n, T;
bool col[maxn + 1], dplus[maxn * 2 + 1], dminus[maxn * 2 + 1];

void queen(int row){
//add something here
}

int main(){
	scanf("%d",&n);
	queen(1);
	printf("%d",T%10000);
	return 0;
}
